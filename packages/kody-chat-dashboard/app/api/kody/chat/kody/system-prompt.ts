/**
 * @fileType barrel
 * @ai-summary Moved to @kody-ade/base/kody-system-prompt (pure prompt
 *   builder, no chat/UI internals). Barrel kept so existing kody-chat
 *   importers keep working.
 */
export * from "@kody-ade/base/kody-system-prompt";
