# Knowledge Graph

Status: **Derived projection**

The Knowledge Graph helps navigation, retrieval, and impact analysis. It is
never authority for definitions, runtime state, permissions, or history.

Every node and edge must retain its source and derivation. Inferred relations
must be distinguishable from direct facts. Changes flow through the source
model and rebuild the graph; graph mutation must not rewrite Agency models.
