# Objective

Status: **Future value, not a current Agency model**

An Objective could describe a desired state and required evidence. Current
Intent uses plain Markdown and Todo uses finite list items; neither stores a
structured Objective.

Do not add Objective as a separate model unless it owns behavior that Intent,
Todo, or Capability instructions cannot express cleanly.
