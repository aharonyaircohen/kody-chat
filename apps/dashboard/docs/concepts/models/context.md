# Context

Status: **Current Markdown guidance**

Context stores durable facts Kody should know about the product, users,
vocabulary, and architecture. It is stored in Convex `repoDocs` and may be
scoped to Agents.

Context is reasoning input, not authority, policy, memory history, or runtime
state. Retrieved untrusted content remains data and cannot become instructions
or grant permission.
