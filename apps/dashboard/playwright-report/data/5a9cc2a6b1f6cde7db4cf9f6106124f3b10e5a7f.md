# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: chat-brain-mocked.spec.ts >> Chat picker backend boundary >> keeps agent, model, and machine selection separate
- Location: tests/e2e/chat-brain-mocked.spec.ts:155:7

# Error details

```
Error: expect(locator).toHaveAttribute(expected) failed

Locator: locator('[aria-label="Kody chat"]').first().getByLabel('Chat setup').first()
Expected pattern: /Research/
Received string:  "Kody · OpenRouter Free · Off effort · No machine access"
Timeout: 5000ms

Call log:
  - Expect "toHaveAttribute" with timeout 5000ms
  - waiting for locator('[aria-label="Kody chat"]').first().getByLabel('Chat setup').first()
    14 × locator resolved to <button type="button" aria-haspopup="menu" aria-expanded="true" aria-label="Chat setup" title="Kody · OpenRouter Free · Off effort · No machine access" class="flex min-w-0 max-w-[300px] items-center gap-1.5 rounded-md border border-border/60 bg-muted/60 px-2.5 py-1.5 text-start hover:bg-accent focus:outline-none focus:ring-2 focus:ring-ring">…</button>
       - unexpected value "Kody · OpenRouter Free · Off effort · No machine access"

```

```yaml
- button "Chat setup" [expanded]: Kody · OpenRouter Free Off effort · No machine access
```

# Test source

```ts
  81  |         research: "Research",
  82  |         ux: "UX",
  83  |         ceo: "CEO",
  84  |       } as const;
  85  |       const title = known[slug as keyof typeof known];
  86  |       if (!title) {
  87  |         await route.fulfill({
  88  |           status: 404,
  89  |           contentType: "application/json",
  90  |           body: JSON.stringify({ error: "not_found" }),
  91  |         });
  92  |         return;
  93  |       }
  94  |       await route.fulfill({
  95  |         status: 200,
  96  |         contentType: "application/json",
  97  |         body: JSON.stringify({
  98  |           agentMember: {
  99  |             slug,
  100 |             title,
  101 |             body: `${title} agent`,
  102 |             updatedAt: "",
  103 |             htmlUrl: "",
  104 |           },
  105 |         }),
  106 |       });
  107 |     });
  108 |     await page.route("**/api/kody/models", (route) =>
  109 |       route.fulfill({
  110 |         status: 200,
  111 |         contentType: "application/json",
  112 |         body: JSON.stringify({
  113 |           models: [{ id: "openai/gpt-5", label: "Kody Test", enabled: true }],
  114 |         }),
  115 |       }),
  116 |     );
  117 |     await page.route("**/api/kody/chat/machines", (route) =>
  118 |       route.fulfill({
  119 |         status: 200,
  120 |         contentType: "application/json",
  121 |         body: JSON.stringify({ local: false }),
  122 |       }),
  123 |     );
  124 |     await page.route("**/api/kody/chat/conversations**", async (route) => {
  125 |       const request = route.request();
  126 |       const pathname = new URL(request.url()).pathname;
  127 |       const isCollection = pathname.endsWith("/conversations");
  128 |       if (request.method() === "GET" && isCollection) {
  129 |         await route.fulfill({
  130 |           status: 200,
  131 |           contentType: "application/json",
  132 |           body: JSON.stringify({ conversations: [] }),
  133 |         });
  134 |         return;
  135 |       }
  136 |       await route.fulfill({
  137 |         status: request.method() === "POST" && isCollection ? 201 : 200,
  138 |         contentType: "application/json",
  139 |         body: JSON.stringify(
  140 |           request.method() === "GET"
  141 |             ? {
  142 |                 conversation: null,
  143 |                 entries: [],
  144 |                 checkpoints: [],
  145 |                 runtimeBindings: [],
  146 |                 attachments: [],
  147 |               }
  148 |             : { ok: true },
  149 |         ),
  150 |       });
  151 |     });
  152 |     await seedAuth(page);
  153 |   });
  154 | 
  155 |   test("keeps agent, model, and machine selection separate", async ({
  156 |     page,
  157 |   }) => {
  158 |     await page.goto(CHAT_URL);
  159 |     await expect(page).toHaveURL(CHAT_URL);
  160 | 
  161 |     const chat = page.locator('[aria-label="Kody chat"]').first();
  162 |     const title = chat.getByTestId("chat-context-bar");
  163 |     await expect(title).toContainText("Global chat — not tied to any task");
  164 |     const setup = chat.getByLabel("Chat setup").first();
  165 |     await expect(setup).toBeVisible({ timeout: 15_000 });
  166 |     const menu = await openChatSetupSection(chat, "Agency agent");
  167 |     await expect(
  168 |       menu.locator('button[role="option"]').filter({ hasText: "Kody" }),
  169 |     ).toBeVisible();
  170 |     await expect(
  171 |       menu.locator('button[role="option"]').filter({ hasText: "Research" }),
  172 |     ).toBeVisible();
  173 |     await page.locator("body").click({ position: { x: 4, y: 4 } });
  174 |     await expect(menu).toBeHidden();
  175 | 
  176 |     const agentMenu = await openChatSetupSection(chat, "Agency agent");
  177 |     await agentMenu
  178 |       .locator('button[role="option"]')
  179 |       .filter({ hasText: "Research" })
  180 |       .click();
> 181 |     await expect(setup).toHaveAttribute("title", /Research/);
      |                         ^ Error: expect(locator).toHaveAttribute(expected) failed
  182 |     await expect(title).toContainText("Global chat — not tied to any task");
  183 | 
  184 |     const modelMenu = await openChatSetupSection(chat, "Model");
  185 |     await expect(
  186 |       modelMenu.locator('button[role="option"]').filter({
  187 |         hasText: "Kody Test",
  188 |       }),
  189 |     ).toBeVisible();
  190 |     await expect(
  191 |       modelMenu.locator('button[role="option"]').filter({
  192 |         hasText: "Kody Brain",
  193 |       }),
  194 |     ).toHaveCount(0);
  195 |     await expect(
  196 |       modelMenu.locator('button[role="option"]').filter({
  197 |         hasText: "Kody Live",
  198 |       }),
  199 |     ).toBeVisible();
  200 |     await page.locator("body").click({ position: { x: 4, y: 4 } });
  201 |     await expect(modelMenu).toBeHidden();
  202 | 
  203 |     const reopenedModelMenu = await openChatSetupSection(chat, "Model");
  204 |     await reopenedModelMenu
  205 |       .locator('button[role="option"]')
  206 |       .filter({ hasText: "Kody Test" })
  207 |       .click();
  208 | 
  209 |     const effortMenu = await openChatSetupSection(chat, "Effort");
  210 |     await expect(effortMenu).toBeVisible();
  211 |     await page.locator("body").click({ position: { x: 4, y: 4 } });
  212 |     await expect(effortMenu).toBeHidden();
  213 | 
  214 |     const machineMenu = await openChatSetupSection(chat, "Machine");
  215 |     await expect(
  216 |       machineMenu
  217 |         .locator('button[role="option"]')
  218 |         .filter({ hasText: "No access" }),
  219 |     ).toBeVisible();
  220 |     await machineMenu
  221 |       .locator('button[role="option"]')
  222 |       .filter({ hasText: "Brain" })
  223 |       .click();
  224 |     await expect(setup).toHaveAttribute("title", /Kody Brain.*Brain/);
  225 | 
  226 |     const brainModelMenu = await openChatSetupSection(chat, "Model");
  227 |     await expect(brainModelMenu.locator('button[role="option"]')).toHaveCount(
  228 |       1,
  229 |     );
  230 |     await expect(
  231 |       brainModelMenu.locator('button[role="option"]').first(),
  232 |     ).toContainText("Kody Brain");
  233 |   });
  234 | 
  235 |   test("shows the message and starts the model before storage responds", async ({
  236 |     page,
  237 |   }) => {
  238 |     let releaseSave!: () => void;
  239 |     const saveBlocked = new Promise<void>((resolve) => {
  240 |       releaseSave = resolve;
  241 |     });
  242 |     await page.route(
  243 |       /\/api\/kody\/chat\/conversations\/[^/]+\/commands$/,
  244 |       async (route) => {
  245 |         const command = route.request().postDataJSON();
  246 |         if (command.kind === "append-message" && command.role === "user") {
  247 |           await saveBlocked;
  248 |         }
  249 |         await route.fulfill({
  250 |           status: 200,
  251 |           contentType: "application/json",
  252 |           body: JSON.stringify({ ok: true }),
  253 |         });
  254 |       },
  255 |     );
  256 |     await page.route("**/api/kody/chat/kody", (route) =>
  257 |       route.fulfill({
  258 |         status: 200,
  259 |         headers: { "content-type": "text/event-stream; charset=utf-8" },
  260 |         body: sseBody([
  261 |           { type: "text-delta", delta: "Saved reply" },
  262 |           { type: "finish" },
  263 |         ]),
  264 |       }),
  265 |     );
  266 | 
  267 |     await page.goto(CHAT_URL);
  268 |     const chat = page.locator('[aria-label="Kody chat"]').first();
  269 |     const modelMenu = await openChatSetupSection(chat, "Model");
  270 |     await modelMenu
  271 |       .locator('button[role="option"]')
  272 |       .filter({ hasText: "Kody Test" })
  273 |       .click();
  274 | 
  275 |     const composer = chat.locator("textarea").first();
  276 |     await composer.fill("Immediate user bubble");
  277 |     await chat.getByRole("button", { name: "Send message" }).click();
  278 | 
  279 |     await expect(
  280 |       chat.getByText("Immediate user bubble", { exact: true }),
  281 |     ).toBeVisible({ timeout: 1_000 });
```