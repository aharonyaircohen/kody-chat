/**
 * Dashboard compatibility barrel for backend-authoritative agent definitions.
 * The implementation belongs to the agency package so every host uses the
 * same storage boundary and versioning contract.
 */
export * from "@kody-ade/agency/agent-files";
