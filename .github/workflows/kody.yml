# Generated by @kody-ade/kody-engine. Do not hand-edit consumer copies.
#
# GitHub authentication fallback:
# 1. GH_PAT or legacy KODY_TOKEN, when configured.
# 2. KODY_APP_ID + KODY_APP_PRIVATE_KEY, minted by the engine.
# 3. GitHub's built-in token, requiring no repository secret.

name: kody

on:
  workflow_dispatch:
    inputs:
      issue_number:
        description: 'GitHub issue number (agent mode)'
        type: string
        default: ''
      sessionId:
        description: 'Chat session ID (chat mode, from Kody Dashboard)'
        type: string
        default: ''
      message:
        description: 'Initial chat message (optional)'
        type: string
        default: ''
      model:
        description: 'Model override (optional)'
        type: string
        default: ''
      dashboardUrl:
        description: 'Dashboard event ingest URL (chat mode)'
        type: string
        default: ''
      capability:
        description: 'Capability action to run'
        type: string
        default: ''
      base:
        description: 'Optional base branch'
        type: string
        default: ''
      runRequest:
        description: 'Canonical Kody execution request JSON'
        type: string
        default: ''
      storeRepoUrl:
        description: 'Company Store repository (owner/repo)'
        type: string
        default: ''
      storeRef:
        description: 'Company Store ref'
        type: string
        default: ''
  issue_comment:
    types: [created]
  pull_request:
    types: [opened, synchronize, closed]
  schedule:
    - cron: '7/15 * * * *'

jobs:
  run:
    if: ${{ github.event_name != 'pull_request' || github.event.action == 'opened' || github.event.action == 'synchronize' || github.event.pull_request.merged == true }}
    runs-on: ubuntu-latest
    timeout-minutes: 360
    concurrency:
      group: kody-${{ inputs.sessionId || inputs.issue_number || github.event.issue.number || github.sha }}
      cancel-in-progress: false
    permissions:
      issues: write
      pull-requests: write
      contents: write
      actions: read
      id-token: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ (github.event.action == 'opened' || github.event.action == 'synchronize') && github.event.pull_request.head.sha || github.event.pull_request.base.ref || github.ref }}
          token: ${{ secrets.GH_PAT || secrets.KODY_TOKEN || github.token }}

      - name: Checkout company Store definitions
        if: ${{ inputs.storeRepoUrl != '' }}
        uses: actions/checkout@v4
        with:
          repository: ${{ inputs.storeRepoUrl }}
          ref: ${{ inputs.storeRef || 'main' }}
          path: .kody-engine/company-store
          token: ${{ secrets.GH_PAT || secrets.KODY_TOKEN || github.token }}

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - env:
          GH_PAT: ${{ secrets.GH_PAT }}
          KODY_TOKEN: ${{ secrets.KODY_TOKEN }}
          E2E_GITHUB_TOKEN: ${{ secrets.E2E_GITHUB_TOKEN }}
          KODY_SERVICE_KEY: ${{ secrets.KODY_SERVICE_KEY }}
          KODY_MASTER_KEY: ${{ secrets.KODY_MASTER_KEY }}
          KODY_APP_ID: ${{ secrets.KODY_APP_ID }}
          KODY_APP_PRIVATE_KEY: ${{ secrets.KODY_APP_PRIVATE_KEY }}
          SESSION_ID: ${{ inputs.sessionId }}
          INIT_MESSAGE: ${{ inputs.message }}
          MODEL: ${{ inputs.model }}
          DASHBOARD_URL: ${{ inputs.dashboardUrl }}
          KODY_PUBLIC_DASHBOARD_URL: ${{ inputs.dashboardUrl }}
          KODY_PUBLIC_E2E_GITHUB_REPO: https://github.com/${{ github.repository }}
          KODY_PUBLIC_LIVE_MUTATION_TARGET: ${{ github.repository }}
          KODY_PUBLIC_LIVE_CONFIRM_MUTATIONS: ${{ github.repository }}
          KODY_PUBLIC_CONVEX_URL: ${{ secrets.CONVEX_URL }}
          KODY_PUBLIC_RUN_REAL_E2E: "1"
          KODY_RUN_REQUEST_JSON: ${{ inputs.runRequest }}
          KODY_DEFINITIONS_ROOT: ${{ inputs.storeRepoUrl != '' && format('{0}/.kody-engine/company-store', github.workspace) || '' }}
          KODY_DEFINITIONS_ROOT_CWD: ${{ inputs.storeRepoUrl != '' && github.workspace || '' }}
        working-directory: ${{ github.workspace }}
        run: |
          npx -y -p @kody-ade/kody-engine@latest kody-engine

      - name: Upload Quality Run evidence
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: kody-quality-${{ github.run_id }}
          path: apps/dashboard/test-results/live-ui-gate/
          if-no-files-found: ignore
          retention-days: 30
