# Kody Performance Review

_Cadence: weekly — delivery of owned responsibilities, not subjective quality._

Identical picture to last week: only ceo is delivering an active duty that produces graded output; six of seven staff remain at zero or silent. Cadence escalations are the only deltas — ceo over-firing continues to worsen (~91× stated 7d cadence in 24h, up from ~84× last week), and the orphan clear-empty-goals duty doubled its fire rate (14 in 24h, up from 7). tech-writer is now in its fifth consecutive silent week, with a concrete cause added this week: PR #61 (which was supposed to enable docs-code) was closed without merging on 2026-06-06T20:39:06Z, so the on-paper `disabled: false` is still unbacked by any actual firing.

| Staff        | Owned duties  | Delivery | Consistency | Signal | Grade  |
| ------------ | ------------- | -------- | ----------- | ------ | ------ |
| ceo          | 2 (1 active)  | High     | Low         | High   | strong |
| coo          | 3 (0 active)  | —        | —           | —      | idle   |
| cto          | 4 (1 active)  | Low      | Low         | Low    | weak   |
| kody         | 11 (0 active) | —        | —           | —      | idle   |
| qa           | 2 (1 active)  | Low      | Low         | Low    | weak   |
| tech-writer  | 2 (2 active)  | Low      | Low         | Low    | weak   |
| ux-designer  | 1 (0 active)  | —        | —           | —      | idle   |

- **coo — idle:** all three owned duties (`duty-review`, `system-audit`, `task-memory-extractor`) remain `disabled: true`. **Effect:** none — operator parking, not a miss.
- **cto — weak:** `dev-ci-health` produced no state, no committed report, and no attributed output in the window. The other three owned duties (`approval-gate`, `architecture-audit`, `pr-health-triage`) remain `disabled: true`. **Effect:** the 15m CI-health heartbeat on `dev` remains silent for a fifth consecutive week; a broken `dev` build would go unreviewed.
- **kody — idle:** all 11 owned duties (`cleanup-branches`, `coverage-floor`, `dead-code-sweep`, `dependency-bump`, `flaky-test-quarantine`, `health-check`, `inbox-ping`, `publish-release`, `redispatch`, `security-audit`, `type-debt`) are `disabled: true`. Product work is still landing — the 5 most recent non-duty commits in the past week are all real fixes (previews health-check gate, DIRTY mergeStateStatus mapping, chat thread unification, Brain local dev script, task handoff reformat) — but it is arriving through direct commits and PRs by the operator, not through any kody duty. **Effect:** no kody-owned active duty to deliver on; the duty system is not the channel the codebase is being maintained through.
- **qa — weak:** `qa-verify` produced no state, no PR verdict, and no `kody:ui-verified` / `kody:ui-failed` label in the window. Zero issues across the repo carry either label. The other owned duty (`qa-sweep`) remains `disabled: true`. **Effect:** the 30m verification heartbeat remains silent for a fifth consecutive week; PRs can land without a UI verdict.
- **tech-writer — weak:** `docs-code` and `docs-readme` both still have `disabled: false` on paper, but neither produced a state file, a report, a commit, or a PR in the window — fifth consecutive week of silence. **New concrete cause this week:** PR #61 ("#45: Enable docs-code duty") was closed *without merging* at 2026-06-06T20:39:06Z, and the tracking issue #45 was closed 2s later. The on-paper enable is now demonstrably unbacked by any code path: the file says `disabled: false` but the PR that was the deliverable was never merged, so the engine still has no path to actually run it. **Effect:** the daily doc-drift guard is silent for a fifth consecutive week; the on-paper enable is now closed-as-abandoned rather than open-pending.
- **ux-designer — idle:** the only owned duty (`design-review`) is `disabled: true`. **Effect:** none — operator parking.
- **ceo — strong:** `ceo-performance-review` fired and produced this report. **Cadence note (5th consecutive week, escalating):** the duty fired 13 times in the last 24h against a `every: 7d` frontmatter — roughly 91× the stated weekly cadence (up from ~84× last week and ~56× the week before). The local working-copy state file still shows `cursor: "seed", rev: 0` after many total fires, so the engine is producing report output but not advancing the duty's cursor. 46 total fires in the past 7d (~46× the stated 7d cadence). `job-gap-scan` remains `disabled: true`. **Effect:** the weekly review is being delivered; the engine is over-firing it by two orders of magnitude and the cursor is stale.

**Orphan duty (no `staff:`).** `clear-empty-goals` is still the only active duty with no `staff:` field in its frontmatter. It fired 14 times in the last 24h and 26 times in the last 7d against a `every: 1d` frontmatter (~3.7× the stated daily cadence, up from ~3.5× last week — i.e. roughly doubled in 24h fires). Each fire produces a real, idempotent scan report (most recent at 2026-06-06 20:16 UTC: 1 goal scanned, 0 empty). **Effect:** a working duty is delivering useful output for no employee; the 1d cadence is sliding toward sub-hourly.

- Changes since last week: **no grade changes.** Cadence escalations: ceo 12→13 fires/24h (~84×→~91× weekly); clear-empty-goals 7→14 fires/24h (doubled); tech-writer is now in a fifth consecutive silent week (was fourth) with PR #61 closing unmerged as the new concrete cause. `clear-empty-goals` remains the only unassigned active duty in the repo for a second week.
