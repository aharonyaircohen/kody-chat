# Kody Performance Review

_Cadence: weekly — delivery of owned responsibilities, not subjective quality._

Four of seven staff delivered no active output this week; only ceo is firing an active duty that produces graded output. kody drops from `steady` to `idle` — `clear-empty-goals`, the de-facto kody duty in last week's report, has no `staff:` field in its frontmatter and is unassigned, so kody's 11 owned duties are all `disabled: true`. cto, qa, and tech-writer active duties remain silent for a fourth consecutive week. ceo cadence note escalates (~84× stated 7d cadence in 24h); engine cursor still stuck at `seed`.

| Staff        | Owned duties  | Delivery | Consistency | Signal | Grade  |
| ------------ | ------------- | -------- | ----------- | ------ | ------ |
| ceo          | 2 (1 active)  | High     | Low         | High   | strong |
| coo          | 3 (0 active)  | —        | —           | —      | idle   |
| cto          | 4 (1 active)  | Low      | Low         | Low    | weak   |
| kody         | 11 (0 active) | —        | —           | —      | idle   |
| qa           | 3 (1 active)  | Low      | Low         | Low    | weak   |
| tech-writer  | 2 (2 active)  | Low      | Low         | Low    | weak   |
| ux-designer  | 1 (0 active)  | —        | —           | —      | idle   |

- **coo — idle:** all three owned duties (`duty-review`, `system-audit`, `task-memory-extractor`) remain `disabled: true`. **Effect:** none — operator parking, not a miss.
- **cto — weak:** `dev-ci-health` produced no state, no committed report, and no attributed output in the window. The `dev` branch does exist (last week's note that it didn't was wrong — `branches` lists it alongside `main` and `kody-state`), so the prior "structurally blocked" framing was inaccurate; the *observable* fact is still the same — zero output, zero commits, zero state advances for a duty on a 15m cadence. The other three owned duties (`approval-gate`, `architecture-audit`, `pr-health-triage`) remain `disabled: true`. **Effect:** the 15m CI-health heartbeat on `dev` remains silent; a broken `dev` build would go unreviewed.
- **kody — idle (reclassified from `steady`):** all 11 owned duties (`cleanup-branches`, `coverage-floor`, `dead-code-sweep`, `dependency-bump`, `flaky-test-quarantine`, `health-check`, `inbox-ping`, `publish-release`, `redispatch`, `security-audit`, `type-debt`) are `disabled: true`. Last week's report treated `clear-empty-goals` as kody's de-facto active duty and graded kody `steady`; that duty has no `staff:` field in its frontmatter, and the kody staff file is explicit ("Every concrete responsibility … lives in the duty that names `staff: kody`"). Per the tick procedure it is unassigned, so kody owns zero active duties and is correctly `idle`. **Side-effect worth surfacing:** `clear-empty-goals` is the only active duty in the repo with no owner — it has fired 7 times in 24h against a 1d cadence and is producing a real, idempotent "no empty goals" report each time, but it is not in any employee's job description. **Effect:** no kody-owned active duty to deliver on; one unassigned active duty is delivering for no employee.
- **qa — weak:** `qa-verify` produced no state, no PR verdict, and no `kody:ui-verified` / `kody:ui-failed` label in the window. Zero issues across the repo carry either label; zero PRs received a merge recommendation from the duty. The other two owned duties (`qa`, `qa-sweep`) remain `disabled: true`. **Effect:** the 30m verification heartbeat remains silent; PRs can land without a UI verdict.
- **tech-writer — weak:** `docs-code` and `docs-readme` both have `disabled: false` on paper, but neither produced a state file, a report, a commit, or a PR in the window. The enabling chain is still open: issue #45 ("Enable docs-code duty") and PR #61 ("#45: Enable docs-code duty") remain open with `kody:done` set, so the on-paper enable remains misleading. The last commit touching either duty file is the 2026-05-29 Prettier sweep — not a docs delivery. **Effect:** the daily doc-drift guard is silent; the on-paper enable is still unbacked.
- **ux-designer — idle:** the only owned duty (`design-review`) is `disabled: true`. **Effect:** none — operator parking.
- **ceo — strong:** `ceo-performance-review` fired and produced this report. The report content is real and graded, so the operational effect on the review itself is nil. **Cadence note (4th consecutive week, escalating):** the duty fired 12 times in the last 24h against a `every: 7d` frontmatter — roughly 84× the stated weekly cadence (up from ~56× last week). The local working-copy state file still shows `cursor: "seed", rev: 0` after many total fires, so the engine is producing report output but not advancing the duty's cursor. `job-gap-scan` remains `disabled: true`. **Effect:** the weekly review is being delivered; the engine is over-firing it by two orders of magnitude and the cursor is stale.

- Changes since last week: **kody `steady`→`idle`** (reclassified — `clear-empty-goals` has no `staff:` field, so last week's `steady` was a loose reading of ownership, not a delivery change). All other staff grades unchanged. ceo cadence note escalates (12 fires / 24h = ~84×, up from ~56× last week); cto's "dev branch missing" reasoning was wrong (the branch exists) but the *result* — zero output — is unchanged.
