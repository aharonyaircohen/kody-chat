# Kody Performance Review

_Cadence: weekly — delivery of owned responsibilities, not subjective quality._

**Week 18.** Same shape as weeks 16 and 17: 5 active duties all run on schedule (~7×/day each, all `outcomeKind: ok`), 3 of them — `dev-ci-health`, `qa-verify`, `docs-readme` — complete without producing their gate artifacts, and the `clear-empty-goals` duty still has zero entries in the activity log. The structural finding from weeks 16 and 17 (`reason: null` on every event) is unchanged. The new finding this week: the structured state machine has now failed to persist for the **third** consecutive tick — input was `cursor: "seed", data: {}` again, so the engine has had three opportunities to start writing `ceo-performance-review.state.json` and has not, meaning the `every: 7d` contract is structurally unenforced from the engine side and will remain so until that file lands on disk.

| Staff        | Owned duties  | Delivery | Consistency | Signal | Grade   |
| ------------ | ------------- | -------- | ----------- | ------ | ------- |
| ceo          | 2 (1 active)  | High     | Med         | High   | steady  |
| coo          | 3 (0 active)  | —        | —           | —      | idle    |
| cto          | 4 (1 active)  | Med      | High        | Low    | unclear |
| kody         | 11 (0 active) | —        | —           | —      | idle    |
| qa           | 3 (1 active)  | Med      | High        | Low    | weak    |
| tech-writer  | 2 (2 active)  | High     | High        | Med    | steady  |
| ux-designer  | 1 (0 active)  | —        | —           | —      | idle    |
| *(unowned)*  | 1 (1 active)  | Low      | Low         | Low    | weak    |

- **cto — unclear (18th week, unchanged):** `dev-ci-health` runs ~7×/day and completes `ok`; still no `kody:dev-ci-red` or `kody:dev-ci-green` issue anywhere in the repo (verified — 0 hits on both label queries), still no `.kody/reports/dev-ci-health.md` on disk, and the only structured state file anywhere is still `.kody/duties/dev-ci-health.state.json` (cursor `idle`, data `{}`, rev 1) — last touched on 2026-06-06T21:30:25Z, **4 days and 50 runs ago**. The activity log records `outcome` but not the reason a tick completed without acting. **Effect:** if `dev` is green, the duty is doing its job and we cannot see it; if it is broken, the operator has not been notified for 18 weeks. Same as weeks 16, 17.
- **qa — weak (18th week, unchanged):** `qa-verify` runs ~7×/day and completes `ok`; still **0** PRs carry `kody:ui-verified` or `kody:ui-failed` (verified — 0 hits on both label queries), and the `kody:cto-decisions` trust-ledger label still does not exist. **9** open PRs still carry `kody:done` and are waiting on a verdict — same set as week 17 (#71, #109, #144, #154, #155, #158, #160, #162, #164, all `kody:done`, all unverified). **Effect:** verdict→merge pipeline remains open-loop; the docs-coverage PRs that `docs-code` cannot dispatch (see tech-writer) and the operator-merged preview fixes (#166, #4dd1c6b0) all bypass the qa-verify gate, so the signal "is this safe to merge?" is still not being answered.
- **clear-empty-goals (unowned, 18th week, unchanged):** still 0 entries in the activity log across 7 sampled days (2026-06-03 → 2026-06-10); the on-disk report at `.kody/reports/clear-empty-goals.md` is still the 2026-06-07 09:53:00Z finding (now 3+ days stale, 6 days since the last duty-driven refresh); the untracked `.kody/goals/{kody-state-split,ai-company-orchestration-7-gap-plan}/state.json` files are still staged/added in the worktree (same as weeks 14, 15, 16, 17). **Effect:** the manifest is being mutated faster than it can be scanned; the unowned duty has now been effectively stopped for four consecutive weeks.

**Changes since last week:** ceo steady→steady; coo idle→idle; cto unclear→unclear; kody idle→idle; qa weak→weak; tech-writer steady→steady; ux-designer idle→idle; *(unowned)* weak→weak. No grade changes this week. The structural state-persistence failure is now three ticks deep, which the prior report flagged as a "foreseeable future" — that future is now.

**Activity log (week 18, 244 events across 7 days):** all 5 active duties ran on schedule (~7×/day each), all `outcome: completed`, all `outcomeKind: ok`, all `reason: null`. Tally per duty (2026-06-03 → 2026-06-10, sampled from `origin/kody-state:.kody/activity/<date>.jsonl`): `qa-verify` = 50, `docs-code` = 50, `docs-readme` = 49, `dev-ci-health` = 50, `ceo-performance-review` = 50. `clear-empty-goals` = 0 across the same window. Per-day totals: 2026-06-03 = 30, 2026-06-04 = 50, 2026-06-05 = 5, 2026-06-07 = 54, 2026-06-08 = 40, 2026-06-09 = 45, 2026-06-10 = 25. (2026-06-06 missing from the log entirely — engine gap, not duty gap, same as week 17.) `docs-readme` missed a single slot (49 vs 50) for the second week running — within tolerance, no signal there.

**State machine (week 18, third tick):** input to this tick was `{"version":1,"rev":0,"cursor":"seed","data":{}}` — the same empty seed state as weeks 16 and 17. The engine has now had three consecutive opportunities to persist `ceo-performance-review.state.json` to the kody-state branch and has not done so. `data.cycle: 1` (week 16), `data.cycle: 2` (week 17), and `data.cycle: 3` (this week) all live only in this report body, not on disk. Until the engine writes the file, the dashboard "next run" readout remains decorative and the over-fire (~7×/day when cadence is 7d) is structurally unfixable from the duty side. Of the 5 active duties, only `dev-ci-health` has a state file on the kody-state branch — and it has not been touched in 4 days (50 runs since the last write).

**Operator-driven work in the window (week 18):** substantive operator activity resumed this week after a flat week 17. Notable main-branch commits: `a52e5e4d chore: upgrade to Next 16`, `2706953f Restore Fly warm pool without shared LiteLLM`, `4dd1c6b0 fix: preserve preview view query params`, `b822970a fix: preserve preview query when joining paths (#166)`, `0de77c8c Add docs management and preview updates`, `c3ef8256 Add memory manager and docs navigation`, `f44f7d72 feat(context): add ideas`, `2757f051 Store reports on kody state branch`, `04123bcc Refine duty control buttons`, plus three `chore(dashboard): update preview environments` commits. None of these are addressed by any active duty (no `qa-verify` verdict, no `dev-ci-health` label, no `docs-readme`/`docs-code` scan of the docs-management additions, no `ceo` reconciliation of the new "memory manager" against the staff inventory).
