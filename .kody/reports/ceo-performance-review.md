# Kody Performance Review

_Cadence: weekly — delivery of owned responsibilities, not subjective quality._

Fifteenth week, picture materially different from week 14: a new `kody-state` branch landed mid-week (operator commit `2757f051` 2026-06-10 04:31Z, "Store reports on kody state branch") and the engine is now writing an authoritative duty activity log to `.kody/activity/{date}.jsonl` on that branch. For the first time this duty can read direct evidence of duty execution — the prior 14 weeks inferred it from labels, PR stamps, and report files. Reading the activity log: **all 5 active duties are running on schedule and completing with `outcomeKind: ok`**, ~7 runs/day each (well above the requested cadence of 15m–1d for the four). Where the duties have been stuck for 14 weeks is not in the running; it is in the **external artifacts they have or have not produced**:

- **tech-writer/docs-code broke through the 14-week "chore verb missing" finding.** Since the 2026-06-09 11:51Z run it has opened 7 new "Doc coverage gap" issues (#151, #153, #157, #159, #161, #163, #165) with the same template body the duty body specifies; 6 of those produced kody:done PRs (#154, #155, #158, #160, #162, #164), all still open. The setup check report (2026-06-09 08:40:30Z) is now stale — the duty has graduated past the per-module coverage check into folder-level "start here" recommendations. Whether this is via the `chore` verb (still not in the engine, per the docs-readme recheck), a `run --issue` substitution, or a Kody-engine update is the open question; the report can't see engine internals.
- **tech-writer/docs-readme has not progressed.** Latest recheck (2026-06-10 00:24:14Z) repeats the same "chore verb still missing" finding; no drift issues opened.
- **cto/dev-ci-health and qa/qa-verify are running but the gate artifacts never appear.** `kody:dev-ci-red` and `kody:cto-decisions` labels still do not exist (15th consecutive week of the same finding). The dev branch hasn't moved since 2026-06-06 09:56Z, so dev-ci-health is plausibly *legitimately* idle — but the duty has no way to surface that idle state to the operator, and we cannot distinguish "dev is green" from "duty is silently failing" from outside.
- **clear-empty-goals (unowned) regressed.** The on-disk report is still 2026-06-07 09:53:00Z (3+ days old), and unlike the other active duties it has **zero entries in the kody-state activity log** — the engine is not running it. Goal 98 ("orchestration contracts") was merged via PR #156 (2026-06-08 14:34:46Z) and a second goal (`ai-company-orchestration-7-gap-plan`) is visible in the worktree's untracked state — neither has been scanned.

| Staff        | Owned duties  | Delivery | Consistency | Signal | Grade   |
| ------------ | ------------- | -------- | ----------- | ------ | ------- |
| ceo          | 2 (1 active)  | High     | Med         | High   | strong  |
| coo          | 3 (0 active)  | —        | —           | —      | idle    |
| cto          | 4 (1 active)  | Med      | High        | Low    | unclear |
| kody         | 11 (0 active) | —        | —           | —      | idle    |
| qa           | 3 (1 active)  | Med      | High        | Low    | weak    |
| tech-writer  | 2 (2 active)  | High     | High        | Med    | strong  |
| ux-designer  | 1 (0 active)  | —        | —           | —      | idle    |
| *(unowned)*  | 1 (1 active)  | Low      | Low         | Low    | weak    |

- **cto — unclear (fifteenth week, still unable to distinguish "dev is green" from "duty is silently failing"):** dev-ci-health is the only active duty (15m cadence, would expect ~672 runs/week; activity log shows ~7 runs/day, ~50/week — well above cadence, all `ok`). No `kody:dev-ci-red` label created; no `.kody/reports/dev-ci-health.md` written; the only state file on the kody-state branch is `dev-ci-health.state.json` rev 1, cursor `idle`, data `{}` — consistent with the duty idling because dev is green, but also consistent with the duty's ensure-label / open-tracking-issue step never running. Dev tip is `84cfd13` (2026-06-06 09:56:59Z, "chore(brain): switch brain to M3 model"); 4 days without a push is plausible for a quiet dev branch, but we can't confirm dev is GREEN without running the duty's check. The activity log only records outcome, not why — that gap is now the load-bearing structural finding. **Effect:** if dev is green, the duty is doing its job and we just can't see it. If dev is broken, the duty is silent-broken for the 15th week and the operator has not been notified.
- **qa — weak (fifteenth week, unchanged on artifacts; activity log shows it is running ~7×/day):** qa-verify is active (30m cadence, ~336 runs/week expected; activity log shows ~50/week, all `ok`). 9 open delivery PRs exist (`#144`, `#154`, `#155`, `#158`, `#160`, `#162`, `#164`, plus 2 operator-originated older ones), **0 of them carry `kody:ui-verified` or `kody:ui-failed`** — same finding as week 14, 13, 12, etc. The `kody:cto-decisions` trust-ledger label still does not exist; no `.kody/duties/qa-verify.state.json` has ever been committed to either branch. The qa-verify reports file is not on disk. **Effect:** the verdict-merge pipeline is open-loop for the 15th week; the auto-merge branch is structurally dead until the operator either creates the label or the duty self-heals; PRs with `kody:done` bypass QA and queue for the operator's manual review.
- **tech-writer — strong (fifteenth week, upgraded from steady; one half broke through, one half didn't):** docs-code went from setup-only (week 14) to producing real work this week — 7 issues opened (#151, #153, #157, #159, #161, #163, #165) with the duty's template body, 6 PRs created (#154, #155, #158, #160, #162, #164), 1 flow-failed (#165, kody:failed — possibly an engine error in the PR-open path). All 6 PRs are open with `kody:done`; none have ui-verified/ui-failed stamps (consistent with QA's weak state). The docs-code report at `.kody/reports/docs-code.md` is still the 2026-06-09 08:40:30Z setup check (stale — the duty has not committed a newer report despite producing real artifacts). docs-readme is unchanged: latest recheck (2026-06-10 00:24:14Z) repeats the chore-verb-missing finding, no drift issues opened. The grade upgrades from `steady` to `strong` because docs-code's 6-PR output is the first sustained duty-driven PR production in 15 weeks, but the same single-half-drives-the-grade pattern from the prior 14 weeks is still present: docs-readme contributes nothing observable. **Effect:** docs-code is now the system's most productive duty by artifact count; docs-readme remains a no-op for the same verb-blocker reason.
- **clear-empty-goals (unowned, fifteenth week, regressed from unclear to weak):** active every 1d. The activity log on kody-state has **no entry for clear-empty-goals** — the engine is not running it, despite the duty body requesting `every: 1d` and the report file path being correct. The on-disk report is still 2026-06-07 09:53:00Z (now 3d 1h stale). The manifest grew in the window: PR #156 (2026-06-08 14:34:46Z, "chore: add goal 98 orchestration contracts") added Goal 98, and a `goal:ai-company-orchestration-7-gap-plan` label is present in the repo (5 open issues #92–#97, 3 closed #90, #91, #95 — 8 issues, none scanned this week). The untracked `.kody/goals/{kody-state-split,ai-company-orchestration-7-gap-plan}/state.json` are still staged/added in the worktree (same as week 14 — the operator has not committed them in 6 days). **Effect:** the unowned duty has effectively stopped; the manifest is being mutated faster than it can be scanned. This is the first week the duty has been a clear regression, not a structural-blocker finding.

**Changes since last week:** tech-writer steady→strong; *(unowned)* clear-empty-goals unclear→weak. ceo strong→strong, coo idle→idle, cto unclear→unclear, kody idle→idle, qa weak→weak, ux-designer idle→idle. The picture of the 5 active duties is: 2 are delivering (ceo, docs-code), 1 is delivering a no-op (docs-readme), 1 is running-but-silent (dev-ci-health), 1 is running-but-silent (qa-verify), 1 has stopped running (clear-empty-goals).

**Kody-state branch — new authoritative execution surface (new this week):** the `kody-state` branch landed 2026-06-10 04:31:59Z (operator commit `2757f051`, "Store reports on kody state branch") and the engine has been writing `.kody/activity/{YYYY-MM-DD}.jsonl` to it since at least 2026-06-08. This is the first time the duty has direct evidence of what the engine actually executed — every prior week relied on inference (label drift, PR stamps, opened issues, report files). The activity log records: `ts`, `action: "Ran duty: <name>"`, `duty`, `staff`, `trigger`, `outcome`, `outcomeKind`, `durationMs`, `runUrl`. It does **not** record why a duty completed without producing a visible artifact (e.g. dev-ci-health "completed" with no `kody:dev-ci-red` label created). **Effect:** the activity log eliminates the "is this duty actually running?" question for 4 of 5 active duties — that question is now closed; the open question is the more useful "why did it complete without observable output?" The reports storage migration is also a work-in-progress: the activity log is on kody-state, but the four reports on `main` (`.kody/reports/{ceo-performance-review,clear-empty-goals,docs-code,docs-readme}.md`) remain the canonical surface for this tick. The duty's grade on the activity log is conservative: it reads `outcomeKind: ok` as "the duty ran and exited without error", not as "the duty did its job" — that distinction is the load-bearing reason the cto and qa grades stay unclear / weak.

**State-observability note (fifteenth week, mostly unchanged):** the kody-state branch has only **one** duty state file (`.kody/duties/dev-ci-health.state.json` rev 1, cursor `idle`, data `{}`). The other 4 active duties (ceo-performance-review, docs-code, docs-readme, qa-verify) have **no** state file on either branch. The activity log is now the de-facto state for those 4 — but it lives in a write-only log, not in the structured `data` field the duty bodies reference. For ceo-performance-review itself, this duty's own state file path is also still missing; the prior report's "the seed state was unchanged at start of this tick" finding continues. **Effect:** the state-observability gap is closing for the 4 duties that have activity logs, but the structured `data.lastRunISO` / `data.lastGrades` discipline that the duty bodies assume is still not on disk anywhere.

**Cadence note (ceo-performance-review, fifteenth week):** the report file has been committed **at least 17 times in the past 9 days** (latest at `2026-06-10T08:51:52Z` prior to this one, now this one at run time), well above the `every: 7d` cadence. Three possible triggers for the over-firing, all consistent with the current evidence: (a) the engine is not respecting the `every: 7d` guard because `lastRunISO` is never written (the seed state is unchanged at start of this tick — same self-finding as the prior 14 weeks), (b) the dashboard now exposes a "Run now" button (consistent with the new `Refine duty control buttons` operator commit `04123bcc` 2026-06-10 04:23Z) and an operator is clicking it, or (c) the cron schedule is being retriggered by the `Store reports on kody state branch` commit because the engine reads the new branch. The honest answer: cannot tell from this duty's data alone. The state file path remains the load-bearing artifact; until it is on disk between ticks, the cadence readout is decorative. **Effect:** next run readout still cannot show a real cadence; cycle counter still never advances through the state path; deltas in this report are hand-computed from the prior report body, not from `data.lastGrades`. This is now week 15 of the same finding.

**Operator-driven work in the window (fifteenth week):** 4 new operator commits in the window: `Add docs management and preview updates` `0de77c8c` (2026-06-10 08:10Z, .kody/docs + .kody/previews/sweep UI), `Add memory manager and docs navigation` (2026-06-10 06:19Z), `feat(context): add ideas` (2026-06-10 05:07Z), plus the `Store reports on kody state branch` infra commit. The 6 doc-coverage-gaps → PRs → kody:done work is all from the docs-code duty, not from the operator. The operator's contribution this week is the *infra* (docs UI, reports branch, memory manager) — not the *work product* — which is the right pattern: the operator builds capabilities, the duties use them.