# Kody Performance Review

_Cadence: weekly — delivery of owned responsibilities, not subjective quality._

Seventh week, no movement in the underlying delivery picture: 4 of 5 active duties still have no on-cadence output, no state file, and (for the two cto/qa ones) no pre-condition the duty depends on. The fifth — tech-writer — landed its long-awaited setup-check reports on both halves today, surfacing the same engine-verb blocker; one operator-driven docs-coverage PR (#160) closed against that finding in the same window, but no duty produced it. clear-empty-goals is still unowned and now ~52h overdue (worse than last weeks ~50h).

| Staff        | Owned duties  | Delivery | Consistency | Signal  | Grade   |
| ------------ | ------------- | -------- | ----------- | ------- | ------- |
| ceo          | 2 (1 active)  | High     | High        | High    | strong  |
| coo          | 3 (0 active)  | —        | —           | —       | idle    |
| cto          | 4 (1 active)  | Unclear  | Unclear     | Unclear | unclear |
| kody         | 10 (0 active) | —        | —           | —       | idle    |
| qa           | 3 (1 active)  | Low      | Low         | Low     | weak    |
| tech-writer  | 2 (2 active)  | Med      | Med         | Med     | steady  |
| ux-designer  | 1 (0 active)  | —        | —           | —       | idle    |

- **cto — unclear (seventh week, unchanged):** dev-ci-health is the only active duty (15m cadence). The `kody:dev-ci-red` label still does not exist in the repo (47 labels checked, including `kody:ui-verified`, `kody:ui-failed`, `kody:docs-coverage`, `kody:fixing-ci`, `kody:reviewing-ui` — none of the cto/dev-ci or kody:cto-decisions labels are present). No `.kody/duties/dev-ci-health.state.json` has ever been committed. **Effect:** seven weeks of identical signal is now structural, not transient — the duty cannot distinguish "dev CI is green and the duty is silently idle by design" from "the duty is being dropped on every wake" without the label it is supposed to create.

- **qa — weak (seventh week, unchanged):** qa-verifys `kody:ui-verified` / `kody:ui-failed` labels **do** exist in the repo, but **zero PRs** in the open + recently-merged set carry either label (verified via `gh api /repos/.../issues?labels=kody:ui-verified` → []; the 30 most recent PRs only carry `kody:done`, no verdict stamps). The `kody:cto-decisions` trust ledger that qa-verifys auto-merge shortcut reads is **still missing** from the repo label set. No `.kody/duties/qa-verify.state.json` has ever been committed. **Effect:** the verdict→merge pipeline is open-loop end-to-end for the seventh week running; every recent delivery PR (#154, #155, #158, #160, plus the 30+ merged in the last 7d) bypasses QA; the auto-merge branch is structurally dead until that label is created.

- **tech-writer — steady (seventh week, unchanged):** docs-codes setup check report landed at 2026-06-09T08:40:30Z and confirmed the coverage heuristic passes — 15/15 candidate folders at or near full coverage, the worst case being `hooks/useMediaQuery.ts` (97.8%) and not load-bearing. docs-readmes setup check landed at 2026-06-09T04:56:53Z with the same engine-verb blocker; the report text claims "Duty is `disabled: true` in the body" but the duty frontmatter at HEAD is `disabled: false` and the only commits to the file since enabling (481863fc) are a prettier sweep, so the reports own "disabled" framing is inaccurate. Both halves converge on the same engine-verb finding: `chore --issue` is not in the engine README, so the duty cannot safely recommend a doc-coverage PR. No `.kody/duties/docs-code.state.json` or `docs-readme.state.json` has ever been committed. **Effect:** both halves are now producing setup checks; both surface real, blocking findings (engine-verb gap) that the operator closed manually via issue #159 → PR #160 (committed at 12:04 UTC, ~20m after the latest ceo refresh) and aed3fca2s `runner-router.ts` folder header — but neither was duty-driven, so the duty remains structurally a no-op for actual drift detection.

- **clear-empty-goals (unowned, seventh week, still drifting):** active every 1d, but the duty frontmatter still has **no `staff:` field**. The last report write was 2026-06-07 09:53 UTC — now ~52h ago, ~28h overdue vs the 1d cadence (worse than last weeks 50h/26h, and the sixth consecutive week of widening drift). The latest report scanned 1 active goal (`kody-state-split`, 5 open tasks #50–#54), closed 0; the untracked `.kody/goals/ai-company-orchestration-7-gap-plan/state.json` (active since 2026-06-07 19:02 UTC) is still not incorporated. **Effect:** the process gap from the prior six weeks is still open and slowly widening; likely owner is coo given their planning/audit posture, but assigning it is the operators call.

**Changes since last week:** none — ceo strong→strong, coo idle→idle, cto unclear→unclear, kody idle→idle, qa weak→weak, tech-writer steady→steady, ux-designer idle→idle. The structural gaps in cto/qa and the unowned clear-empty-goals are now in week seven. The only new shape in the window is manual operator activity (issue #157 → PR #158 dispatched via `@kody run`, issue #159 → PR #160 closed against a docs-coverage gap, aed3fca2s folder header) — none of it duty-driven.

**Structural note (cross-cutting, unchanged from the last six weeks):** of the 5 active duties, 2 depend on labels that still do not exist in the repo (`kody:dev-ci-red` for cto/dev-ci-health, `kody:cto-decisions` for qa/qa-verifys auto-merge branch). The other 3 (tech-writers two halves + clear-empty-goals) depend on either an engine verb that doesnt exist (`chore --issue`) or an owner who doesnt exist (clear-empty-goals has no `staff:` field). When a dutys own `gh label create` / `engine verb add` / `staff: <slug>` step does not run — or runs in a path that silently swallows its own error — the duty becomes structurally unable to deliver without surfacing a visible failure. The coo-owned duty-review and system-audit would catch this pattern at the design level if/when they are enabled.

**State-observability note (cross-cutting, unchanged from the last six weeks):** of the 5 active duties, **none** of cto/dev-ci-health, qa/qa-verify, tech-writer/docs-code, or tech-writer/docs-readme has a `.kody/duties/<slug>.state.json` ever committed to the repo. The ceo-performance-review itself is also still missing state between ticks. **Effect:** every active staffs "is this duty actually running?" question is answered by inference (label drift, PR stamps, opened issues) rather than by direct evidence.

**Self-finding (ceo-performance-review, seventh week, unchanged):** the prior state shape (`{ cursor: "seed", data: {} }`) — now confirmed unchanged at start of this tick — means the closing block is not being persisted between ticks. The report file has been committed 10 times in the past 24h (latest at 2026-06-09T11:44:30Z; the docs-code setup check at 08:40:30Z, docs-readme setup check at 04:56:53Z, and the operator-driven aed3fca2 at 12:04:00Z bookend the window), well above the `every: 7d` cadence — the engine re-fires on every wake because `lastRunISO` is never written. **Effect:** "next run" readout cannot show a real cadence; cycle counter never advances through the state path; deltas in this report are hand-computed from the prior report body, not from `data.lastGrades`. This is now week 7 of the same finding.
